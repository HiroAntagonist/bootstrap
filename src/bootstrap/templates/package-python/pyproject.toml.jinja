[project]
name = "{{ package_name }}"
version = "0.1.0"
description = "{{ package_name }} package"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
