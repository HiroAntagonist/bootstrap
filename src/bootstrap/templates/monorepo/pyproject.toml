[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "Monorepo for {{ project_name }}"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.9.0",
    "httpx>=0.25.0"
]

[project.scripts]
{{ project_name }}-cli = "cli.main:app"

[tool.uv.workspace]
members = ["backend"]
