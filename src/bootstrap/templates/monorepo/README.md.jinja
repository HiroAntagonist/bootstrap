# {{ project_name }}

Generated by Bootstrap CLI.

## Overview

- **Backend**: {{ backend_type }}
- **Deploy**: {{ deploy_target }}

## Getting Started

### Prerequisites

- `uv` (Python package manager)
- `go` (If using Go backend)
- `task` (Taskfile runner)
- `docker` (For local services)

### Setup

```bash
task setup
```

### Development

Start the development server and local services:

```bash
task dev
```

### Testing

Run the test suite:

```bash
task test
```

## Directory Structure
- `backend/`: Application source code.
- `cli/`: Management scripts (Python).
- `infra/`: Infrastructure configuration (Fly.io, Docker).
- `api/`: OpenAPI specifications.
- `packages/`: Shared libraries and generated clients.
