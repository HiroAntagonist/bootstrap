# Ignore generated source code (regenerated by task generate:api)
src/{{ project_name_sanitized }}_api/*
# Keep the placeholder init file so the package is installable before generation
!src/{{ project_name_sanitized }}_api/__init__.py
